-- Add category and priority columns to assets table
ALTER TABLE assets
ADD COLUMN IF NOT EXISTS category TEXT,
ADD COLUMN IF NOT EXISTS priority TEXT;

-- Optional: Add check constraints for valid values
ALTER TABLE assets
ADD CONSTRAINT valid_category CHECK (
  category IS NULL OR category IN ('art', 'code', 'audio', 'design', 'documentation', 'marketing', 'infrastructure', 'other')
);

ALTER TABLE assets
ADD CONSTRAINT valid_priority CHECK (
  priority IS NULL OR priority IN ('low', 'medium', 'high', 'critical')
);
